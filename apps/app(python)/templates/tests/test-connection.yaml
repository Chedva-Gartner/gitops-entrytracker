apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "app-entryTracker.fullname" . }}-test-connection"
  labels:
    {{- include "app-entryTracker.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "app-entryTracker.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
